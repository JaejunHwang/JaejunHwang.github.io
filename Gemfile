source 'https://rubygems.org'

# If you have any plugins, put them here!
group :jekyll_plugins do
    gem 'jekyll'
    gem 'jekyll-archives'
    gem 'jekyll-diagrams'
    gem 'jekyll-email-protect'
    gem 'jekyll-feed'
    gem 'jekyll-github-metadata'
    gem 'jekyll-imagemagick'
    gem 'jekyll-paginate-v2'
    gem 'jekyll-scholar'
    gem 'jekyll-sitemap'
    gem 'jekyll-target-blank'
    gem 'jekyll-twitter-plugin'
    gem 'jemoji'
    gem 'unicode_utils'
    gem 'webrick'
    gem 'htmlcompressor'
    gem 'htmlbeautifier'
    gem 'jekyll-seo-tag'
    gem 'jekyll-relative-links'
    gem 'jekyll-optional-front-matter'
    gem 'jekyll-readme-index'
    gem 'jekyll-default-layout'
    gem 'jekyll-titles-from-headings'
    gem 'jekyll-coffeescript'
    gem 'jekyll-commonmark-ghpages'
    gem 'jekyll-gist'
    gem 'jekyll-paginate'
end

# Windows and JRuby does not include zoneinfo files, so bundle the tzinfo-data gem
# and associated bundle platform to prevent Jekyll from aborting.
platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", ">= 1", "< 3"
  gem "tzinfo-data"
end

# Performance-booster for watching directories on Windows
gem "wdm", "~> 0.1.1", :platforms => [:mingw, :x64_mingw, :mswin]

# Lock `http_parser.rb` gem to `v0.6.x` on JRuby builds since newer versions of the gem
# do not have a Java counterpart.
# See https://github.com/eventmachine/eventmachine/issues/537
# If you have any issues with `http_parser.rb`, please report them to https://github.com/eventmachine/eventmachine/issues/537
gem "http_parser.rb", "~> 0.6.0", :platforms => [:jruby]
